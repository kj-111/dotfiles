# Exercism CLI

Setup op een nieuwe Mac:

```sh
brew install exercism
exercism configure --token='<token>'
```

Haal de token uit [Exercism Settings](https://exercism.org/settings/api_cli).
Dit maakt `user.json`; dat bestand bevat de token en wordt daarom niet gecommit.
